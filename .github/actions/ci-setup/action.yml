name: 'CI setup'
runs:
  using: 'composite'
  steps:
    - name: Setup Node.js 18.x
      uses: actions/setup-node@v3
      with:
        node-version: 18.x
        cache: yarn

    - name: Install Dependencies
      run: npm install --no-save
      shell: bash
